# Groq — free key at https://console.groq.com/keys (open-weights Llama models)
GROQ_API_KEY=gsk_...

# OpenAI — only needed for the gpt-4o-mini option
OPENAI_API_KEY=sk-proj-...

# Optional: point at an artifacts bundle outside the repo
# RAG_ARTIFACTS=/path/to/artifacts
