# Serving dependencies only. The research project (Chroma, matplotlib, jupyter,
# scikit-learn, nbconvert) is deliberately NOT required here -- retrieval at this
# scale is a numpy matmul, so there is no vector database to deploy.

streamlit>=1.43        # chat_input(accept_file=...) — inline image attachment
numpy>=1.26
pandas>=2.2
pyarrow>=15.0
pillow>=10.0
python-dotenv>=1.0

# CLIP encoders, CPU build. The default wheels are CUDA and run ~2.5GB, which
# exceeds Streamlit Cloud's image limit; one uploaded image is a single forward
# pass, so CPU is the right target for serving.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2
torchvision>=0.17      # transformers imports it when resolving image processors
transformers>=4.40,<6

# LLM providers
langchain-core>=0.3
langchain-openai>=0.2
langchain-groq>=0.2
